# FlashCut

A Chrome extension for creating flashcards from selected web content, powered by AI.

## Project Structure

- **extension/** - Chrome extension frontend
- **server/** - Node.js/Express backend
- **migrations/** - Database initialization scripts

## Getting Started

### Prerequisites
- Node.js and npm
- Chrome browser for testing the extension

### Installation

1. Install root dependencies: `npm install`
2. Install extension dependencies: `cd extension && npm install`
3. Install server dependencies: `cd ../server && npm install`

## Development

See individual README files in extension/ and server/ directories.
