{
  "name": "flashcut-server",
  "version": "1.0.0",
  "description": "Backend server for FlashCut",
  "main": "src/server.js",
  "scripts": {
    "dev": "node src/server.js",
    "build": "echo 'Build server'",
    "test": "echo 'Run tests'"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.80.0",
    "cors": "^2.8.6",
    "dotenv": "^16.6.1",
    "express": "^4.18.2",
    "openai": "^3.2.1",
    "pg": "^8.9.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
